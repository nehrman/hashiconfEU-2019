apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: fruits-certificate
  namespace: fruits-catalog
spec:
  secretName: fruits-certificate
  issuerRef:
    name: vault-issuer
    kind: issuer
  commonName: testlab.local
  dnsNames:
  - fruits.testlab.local